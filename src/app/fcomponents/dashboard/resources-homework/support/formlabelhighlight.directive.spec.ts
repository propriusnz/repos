import { FormlabelhighlightDirective } from './formlabelhighlight.directive';

describe('FromlabelhighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FormlabelhighlightDirective();
    expect(directive).toBeTruthy();
  });
});
